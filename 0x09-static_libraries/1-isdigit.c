#include "main.h"

/**
 * _isdigit-check if character is numeric
 * @d:character to check if numerical digit is passed
 * Return:1-if numerical character is passed, 0-if not.
 */
int _isdigit(int c)
{
	if ('isdigit(d)' == 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
